// @flow
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  button: {
    alignItems: 'center',
    borderRadius: 2,
    padding: 8,
    justifyContent: 'center',
  },
  disabled: {
    opacity: 0.5,
  },
});
