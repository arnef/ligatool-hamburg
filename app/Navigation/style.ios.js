import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    leftButton: {
        padding: 10,
    },
    title: {
        marginTop: 12,
    },
    toolbar: {
        
    }
})