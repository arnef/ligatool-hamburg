import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  emptyListText: {
    padding: 16,
    textAlign: 'center',
  },
  activityIndicator: {
    justifyContent: 'center',
    flex: 1,
  },
});
